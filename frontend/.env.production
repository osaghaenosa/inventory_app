# Production build — frontend and backend served from the same host.
# Use relative /api so the correct server is always called regardless of domain.
# DO NOT set VITE_API_URL here unless your frontend and backend are on different domains.
